# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist - IMPORTANT: Do NOT ignore dist folder!
# The dist folder must be committed for GitHub package installations to work.
# When users install via "npm install github:votuandi/divt-text-editor",
# they need the pre-built files since npm doesn't run build scripts.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
